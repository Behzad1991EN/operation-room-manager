{
  "name": "operation-room-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first operating-room personnel scheduling",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "highs": "1.15.3"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
